const Counter = ({ counter }) => {
  return (
    <div className="counter fs-1 bg-info d-inline-block mb-5 text-white py-2 px-5 rounded-5">
      {counter}
    </div>
  );
};

export default Counter;
